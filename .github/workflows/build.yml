name: 🏗️ Build Power Angel v8.0

on:
  push:
    paths:
      - 'AngelSmartMichael.cpp'
      - '.github/workflows/**'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup MSVC
      uses
